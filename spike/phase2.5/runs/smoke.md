进入自演进 DEC003，基本盘 F001
## 任务
- T00001 read_paper done $0.43 q5 None→0.23  P001 这篇论文之前已经有人（T00004）精读过，笔记和 E001–E005 已覆盖主要内容。所以这次我重读全文，只找没被记过的点，新记了一条证据：**E006**（支持 A001，强度弱）。它的内容是：下层不会死板地照着草图走，而是结
- T00002 incubate done $0.18 q5 0.23→0.23 质疑 Q001 里“interface 已给定”的隐含含义——给定的是 interface 的形态，而不是它的误差。H001/H002/H003 都把瓶颈放在下层表示或感知上，没有考虑上层经 interface 下传的空间信息本身带有误差，而精细操作的容差可能比这个误差还小。 我登记了一条想法（I001）。 **切入角度**：Q001 说 interface 是给定的。我认为这只固定了 interface 的形态，没有固定它的误差。H001、H002、H003 都在下层内部找瓶颈，分别是动作的时间粒度、接触语义、
- T00003 incubate done $0.15 q5 0.23→0.23 不看瓶颈在哪一种信息缺失（H001-H003），也不看 interface 的误差（T00002），而是看下层在训练中怎样分配对不同输入的依赖。演示轨迹大部分是自由空间运动，这段里 interface 几乎能决定 action；接触段很短，决定 action 的是局部 observation。模型可能学成“全程以 interface 为主”的捷径，到了精细阶段仍然听 interface、忽略自身观测。 这条推演已经做完，登记了一条想法 I002。 **切入角度**：H001/H002/H003 都把瓶颈归到“缺什么”：缺时间粒度、缺接触语义、缺接触闭环。T00002 看的是 interface 本身的误差。我换了一个问题：下层在训练中怎样
- T00004 ground_idea done $0.78 q5 0.23→0.25  I001 已核查完，结论是 **prior_work（有相近工作，没有被直接反驳）**，记为 GR003。本次记了 3 条证据（E007–E009），新登记了 P007。 - **没有直接反驳。** 最接近证伪条件的是 RT-Afforda
- T00005 ground_idea done $0.84 q5 0.25→0.28  I002 的结论标为 **prior_work**（GR004）：没有找到直接反驳它的原文，但有几项相近的已有工作。这个判断不牢，因为 arXiv 接口一直报 406，新登记的三篇论文都没有全文，没能逐段核查。 **相近工作（只读到元数据）

总花费 $2.37

## 推演链实际调用的工具（只看 stream）
- T00002 init tools: ['Glob', 'Grep', 'Read', 'mcp__state__check_dead_ends', 'mcp__state__checkpoint', 'mcp__state__record_idea']
- T00002 调用：{'Read': 1, 'mcp__state__checkpoint': 3, 'mcp__state__check_dead_ends': 1, 'mcp__state__record_idea': 1}
  - --restricted=True --add-dir=False
- T00003 init tools: ['Glob', 'Grep', 'Read', 'mcp__state__check_dead_ends', 'mcp__state__checkpoint', 'mcp__state__record_idea']
- T00003 调用：{'Read': 1, 'mcp__state__checkpoint': 3, 'mcp__state__check_dead_ends': 1, 'mcp__state__record_idea': 1}
  - --restricted=True --add-dir=False

## Ideas
### I001（shortlisted，N=4，接地 prior_work，未登记前提 2）

精细操作的一个主要失败来源不在下层的动作表示或接触感知，而在于下层对 interface 的信任度校准错了：上层下传的空间信息误差比精细任务的容差大，而下层在训练时只见过准确的 interface，于是部署时忠实执行一个错的目标。“interface 给定”只固定了它的形态，没有固定它的误差；下层该学的是把 interface 当粗先验，再用自身观测做局部修正。

- 证伪：同一个下层模型，把上层实际输出的 interface 换成 oracle（真值）interface 之后，接触密集的精细任务成功率几乎不变；或者训练时按上层实测误差分布扰动 interface 之后，部署成功率没有提升。出现任一情况，都说明 interface 误差/信任错配不是瓶颈。另外，如果实测上层 interface 的空间误差本来就在任务容差之内，这个想法的前提就不成立。
- 前提 A002：上层经 interface 下传的内容里含有空间量（位置、关键点、目标区域或位姿），不只是符号化的任务描述
- 前提 A003：上层视觉模型的空间误差量级大于精细操作任务的容差（例如厘米级对毫米级）
- 前提 A004：下层训练时常用的 interface 是从演示回溯标注出来的准确值，而不是上层模型的真实输出，因此训练和部署时 interface 的误差分布不一致
- 前提 A005：下层自己的观测（例如腕部相机、近距离视角）足以在局部把误差修正到容差之内
- 挑战：[] 无
- 关系：['Q001', 'A001'] ['H001', 'H002', 'H003', 'A001', 'U001']
- 接地 GR003（prior_work，refs ['P002', 'P006', 'P007', 'E007', 'E008', 'E009']）：【直接反驳】没有找到能直接反驳的原文。最接近证伪条件的是 RT-Affordance（P006，E008）：同一个下层策略用 oracle affordance 和预测 affordance，抓取成功率是 76% 对 68%，放置/铰接任务是 70% 对 70%；oracle 条件下的失败，作者归因于下层跟随 affordance 时不精确。这与“oracle 替换后几乎不变”的证伪条件方向一致，但任务是粗粒度的抓放，每格只有 5 次试验，不是接触密集的毫米级任务，所以只记为 weak contradict，不足以推翻 I001 在精细任务上的主张。它更像是一条边界：在粗粒度任务上，interface 误差不是瓶颈。
【相近工作】(1) HAMSTER（P002 sa2.2-p2，E007）：下层用真值路径训练、部署时用 VLM 预测路径，作者明确为了对 VLM 预测误差鲁棒，在训练时给路径点加 N(0,0.01) 噪声。这正是 I001 提出的补救（训练时扰动 interface）的先例。不同之处：噪声是固定的小高斯，没有按上层实测误差分布匹配；没有消融；也没有针对精细任务的容差分析。它同时说明 A004 里“下层只见过准确 interface”并不普遍成立，已有工作会加噪。(2) Coarse-to-Fine Imitation Learning（P007，Johns ICRA

提交历史（看接地是否改写正文）：
ar-agent idea I001: 接地 GR003 → shortlisted
ar-agent evidence: 记录证据：E008 → I001
ar-agent idea: 推演链登记 Idea：I001（新前提 4 条：A002, A003, A004, A005）

### I002（shortlisted，N=4，接地 prior_work，未登记前提 2）

即使 interface 完全准确，下层 action model 也可能因为输入依赖分配不当而做不好精细操作。演示里大部分时间步是自由空间运动，这段中 interface 几乎决定了 action；接触段很短，决定 action 的是下层自己的局部观测。按时间步平均的模仿学习会让模型学成“全程听 interface”的捷径，结果到了接触段，局部观测里即使已经有需要的信息也用不上。按这个看法，精细操作的一部分瓶颈是“有信息但没被用上”，不是“缺信息”（H002/H003）或“缺表达带宽”（H001）。

- 证伪：以下任一情况成立，本想法即错。(a) 在接触段对 interface 施加扰动或屏蔽，action 的变化远小于扰动局部观测/本体感觉时的变化，说明模型在接触段本来就以局部观测为主。(b) 从同一个接触前状态出发，有 interface 条件的策略与屏蔽 interface（或接触段对 interface 做 dropout）训练出的策略，在接触子段的成功率没有差别。(c) 只调整依赖分配（给接触段加大损失权重、接触段做 interface dropout），不增加任何新的传感输入，精细任务成功率没有提升。
- 前提 A006：通用操作演示中，自由空间运动段占时间步的大多数，接触/精细段占少数
- 前提 A007：与任务无关的 interface 主要携带粗粒度空间与任务信息，对自由运动段 action 的预测力远高于对接触段 action 的预测力
- 前提 A008：下层训练目标是按时间步平均的模仿学习损失（行为克隆类），没有按阶段重新加权
- 前提 A009：在至少一部分精细任务中，下层现有的局部观测（腕部相机/本体感觉）已经含有完成接触段所需的信息
- 挑战：[] 无
- 关系：['Q001', 'A001'] ['H001', 'H002', 'H003', 'I001']
- 接地 GR004（prior_work，refs ['P009', 'P010', 'P008', 'P007', 'E010']）：【直接反驳】没有找到。没有看到原文触发它的证伪条件 (a)(b)(c)，也没有看到原文推翻 A006–A009。限制：arXiv API 全程返回 406，新登记的 P008/P009/P010 都只有元数据或链接，没有全文，无法逐段核查；已登记论文（P001–P006）里没有接触段依赖分配的实验。唯一记下的证据是 E010（P003 s4.5-p2，neutral/weak）：上层 VLM 在难任务里“输入已含信息却被忽略”，形态相近，但层级与机制都不同。

【相近工作（线索，只读了元数据，未读全文）】
(1) P009 Keyframe-Focused Visual IL（Wen et al., ICML 2021）：BC 中的捷径/copycat 问题，由按时间步平均的损失造成；补救办法是给动作变化点（关键帧）加大损失权重。这与 I002 的机制和证伪实验 (c)“接触段加大损失权重”几乎同构。区别：它的捷径来自观测历史（copycat 前一步动作），不是来自上层 interface；实验场景是驾驶和 MuJoCo，不是接触操作。
(2) P010 MS-Bot（Feng et al., CoRL 2024）：按任务阶段动态调整视觉/听觉/触觉各模态的优先级（倒水、带键槽的插销）。这与“不同阶段应依赖不同输入”一致。区别：它的动机是多模态融合，不涉及上层 interface，

提交历史（看接地是否改写正文）：
ar-agent idea I002: 接地 GR004 → shortlisted
ar-agent evidence: 记录证据：E010 → I002
ar-agent idea: 推演链登记 Idea：I002（新前提 4 条：A006, A007, A008, A009）


## 基本盘
ar-system foundation：F002 ← F001：+E007 E008 E009 E010（GR003 GR004）
ar-system foundation: 进入自演进时取首版基本盘：F001

### 推演记录 T00002（done）

# 推演记录 T00002

## 切入角度

质疑 Q001 里“interface 已给定”的隐含含义——给定的是 interface 的形态，而不是它的误差。H001/H002/H003 都把瓶颈放在下层表示或感知上，没有考虑上层经 interface 下传的空间信息本身带有误差，而精细操作的容差可能比这个误差还小。

## 过程笔记（checkpoint）

- 2026-09-23T23:41:17 角度：质疑 Q001 里“interface 已给定”的隐含含义——给定的是 interface 的形态，而不是它的误差。H001/H002/H003 都把瓶颈放在下层表示或感知上，没有考虑上层经 interface 下传的空间信息本身带有误差，而精细操作的容差可能比这个误差还小。
- 2026-09-23T23:41:25 推演：精细操作的定义就是容差小（毫米级）；上层视觉模型下传的空间量（关键点/目标位姿/区域）误差可能是厘米级。如果下层训练时用的是从演示回溯出来的准确 interface 标注，它学到的是“照 interface 做”，部署时就会忠实地执行一个带误差的目标。这个失败发生在接触之前，H003 的接触闭环救不了。在范围内能动的：让下层把 interface 当作粗先验，用自己的近距离观测做局部重定位（例如训练时注入与上层实测误差分布匹配的 interface 扰动）。准备登记。
- 2026-09-23T23:41:39 已登记 I001（interface 信任度校准错配），新前提 A002–A005。本条推演结束，只登记这 1 条。

## 最终回复

我登记了一条想法（I001）。

**切入角度**：Q001 说 interface 是给定的。我认为这只固定了 interface 的形态，没有固定它的误差。H001、H002、H003 都在下层内部找瓶颈，分别是动作的时间粒度、接触语义、接触闭环。它们都默认下层收到的目标本身是对的。

**推演**：按 A001，interface 与任务无关，所以它的空间精度取决于通用的上层视觉模型，不会为某个精细任务专门调高。精细操作的定义就是容差小。如果上层下传的位置或位姿偏差大于这个容差，那么失败在接触之前就已经发生，更高的动作频率和接触闭环都补救不了。另一方面，下层训练时用的 interface 往往是从演示回溯标注出来的准确值，所以它学会的是按 interface 照做，部署时就会去执行一个错的目标。在"不改 interface"的范围内，下层能改的只有对待 interface 的方式：把它当粗略的先验，再用自己的近距离观测做局部修正。一种做法是训练时对 interface 注入与上层实测误差分布相符的扰动。

**登记内容 I001**：精细操作失败的一个主要来源，是下层对 interface 的信任程度没有校准好。
- **证伪条件**：把上层的实际输出换成真值 interface 后，成功率几乎不变；或者训练时加入扰动后，部署成功率没有提升；或者上层实测的空间误差本来就在任务容差之内。
- **新前提（4 条，A002–A005）**：
  - interface 里含空间量；
  - 上层误差的量级大于任务容差；
  - 训练和部署时 interface 的误差分布不一致；
  - 下层自己的观测足以做局部修正。

  这几条都没有检验过，这个想法的分量基本就压在它们上面。
- **与已有假设的关系**：它与 H001、H003 互补，两者管接触之后，它管接触之前。检验时可以在已有的交叉实验里再加一个因子：真值 interface 对比上层实际输出的 interface。它与 H002 方向相反：H002 要往 interface 里加信息，这里认为 interface 已有的空间信息被信任得过头了。U001 的仿真折扣同样适用，因为仿真里的上层误差分布未必代表真机。

### 推演记录 T00003（done）

# 推演记录 T00003

## 切入角度

不看瓶颈在哪一种信息缺失（H001-H003），也不看 interface 的误差（T00002），而是看下层在训练中怎样分配对不同输入的依赖。演示轨迹大部分是自由空间运动，这段里 interface 几乎能决定 action；接触段很短，决定 action 的是局部 observation。模型可能学成“全程以 interface 为主”的捷径，到了精细阶段仍然听 interface、忽略自身观测。

## 过程笔记（checkpoint）

- 2026-09-23T23:42:07 角度：不看瓶颈在哪一种信息缺失（H001-H003），也不看 interface 的误差（T00002），而是看下层在训练中怎样分配对不同输入的依赖。演示轨迹大部分是自由空间运动，这段里 interface 几乎能决定 action；接触段很短，决定 action 的是局部 observation。模型可能学成“全程以 interface 为主”的捷径，到了精细阶段仍然听 interface、忽略自身观测。
- 2026-09-23T23:42:16 推演：(1) 假设 A001 成立，与任务无关的 interface 主要携带粗粒度的空间和任务信息，所以它在自由运动段能强烈预测 action，在接触段只能弱预测。(2) 行为克隆的损失按时间步平均，接触段步数少，模型会学到“以 interface 为主”的捷径。(3) 这和 H003(1) 不同：即使局部观测或力信号里已经有需要的信息，模型也可能用不上。可检验的推论是：按 H003 加入力输入、却不改变依赖分配时，收益会低于预期。(4) 干净的检验：从同一个接触前状态出发，比较有 interface 条件和去掉/屏蔽 interface 的单任务策略在接触子段上的表现；再测接触段里 action 对 interface 扰动与对局部观测扰动的敏感度。下一步：check_dead_ends，然后登记。
- 2026-09-23T23:42:35 已登记 I002（interface 捷径依赖，属于依赖分配问题），本条推演结束，不再登记第二条。

## 最终回复

这条推演已经做完，登记了一条想法 I002。

**切入角度**：H001/H002/H003 都把瓶颈归到“缺什么”：缺时间粒度、缺接触语义、缺接触闭环。T00002 看的是 interface 本身的误差。我换了一个问题：下层在训练中怎样分配对各路输入的依赖。

**推理过程**：先假设 A001 成立。与任务无关的 interface 大概只携带粗粒度的目标和空间信息。这类信息在自由空间运动段几乎能决定 action，到接触段预测力就很弱。演示轨迹里自由运动占大多数时间步，而行为克隆按时间步平均损失，所以训练会奖励“全程听 interface”这条捷径。结果到了接触段，即使腕部相机或本体感觉里已经有需要的信息，模型也用不上。瓶颈于是有一部分是“信息有了但没被用上”，而不是缺信息。

**登记的想法 I002**：
- **和已有假设的关系**：
  - 与 H002/H003 竞争。它还推出一个可检验的预测：按 H003 直接加力或触觉输入，如果不改变依赖分配，收益会被同一种捷径削弱。
  - 与 I001 机制不同。I001 需要 interface 有误差，I002 在 interface 完全准确时也成立，所以用准确的 interface 做实验就能区分两者。
- **什么情况下它是错的**，以下任一成立即可：
  - 接触段里，模型的 action 对 interface 扰动不敏感，对局部观测扰动敏感，说明它本来就以局部观测为主。
  - 从同一个接触前状态出发，屏蔽 interface 或在接触段对 interface 做 dropout，接触子段的成功率没有差别。
  - 只调整依赖分配、不加任何新传感器，比如给接触段加大损失权重，成功率没有提升。
- **新引入的前提有 4 条**，工具编号为 A006–A009，都未经检验：
  - 演示中自由运动段占多数时间步；
  - interface 对不同阶段 action 的预测力不均匀；
  - 训练用的是按时间步平均的行为克隆损失；
  - 下层现有的局部观测在部分任务里已经含有接触段需要的信息。

  其中最后一条最关键，也最可能不成立。如果局部观测根本不含所需信息，I002 就退化为 H003。

我没有再登记第二条，因为没有其他足够成形的方向。


校验错误：[]
